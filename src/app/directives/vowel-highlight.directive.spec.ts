import { VowelHighlightDirective } from './vowel-highlight.directive';

describe('VowelHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new VowelHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
